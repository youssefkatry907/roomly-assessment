/**
 * The wire shape of a booking.
 *
 * TODO(candidate): decide what belongs here, write the mapper, and use it.
 * The aggregate itself is not a response body. Note the brief: organiser
 * identity on the room schedule is visible to managers only.
 */
export class BookingResponse {
  // TODO(candidate)
}
